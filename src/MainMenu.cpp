#include "MainMenu.hpp"
#include "GameConfig.hpp"

GameConfig MainMenu::show(SpriteBuffer &screen) {
    GameConfig gc{};

    /*
    std::cin >> gc.levelIndex;


    if (levelIndex) {
      // reporta erro
      // fecha o programa
      std::exit(1);
    }
    */

    return gc;
}